services:
  coinflip:
    build: .
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
